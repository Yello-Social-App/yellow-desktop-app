Sample data for surfaces the Yello API does not serve yet — stories and the
people directory. Each store seeds from here and keeps its mutations in
memory for the session; nothing is sent anywhere. Swap a store's seed for an
API call when the endpoint ships and the screens do not change.
